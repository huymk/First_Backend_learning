
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>get CRUD</title>
    
</head>
<style>
  table {
    font-family: arial, sans-serif;
    border-collapse: collapse;
    width: 100%;
  }
  
  td, th {
    border: 1px solid #dddddd;
    text-align: center;
    padding: 8px;
  }

  .btn-edit, .btn-delete {
    outline:none;
    border:none;
    border-radius: 3px;
    background-color: orange;
    height: 25px;
    width: 60px;
    cursor: pointer;
  }

  .btn-delete {
    background-color: red;
  }  
</style>
<body>
  <table>
        <tr>
          <th>Email</th>
          <th>First Name</th>
          <th>Last Name</th>
          <th>Adress</th>
          <th>Actions</th>

        </tr>
    <% for(let i=0; i < dataTable.length; i++) { %>
       <tr>
         <td><%= dataTable[i].email %></td>
         <td><%= dataTable[i].firstName %></td>
         <td><%= dataTable[i].lastName %></td>
         <td><%= dataTable[i].address %></td>
         <td>
          <a href="/edit-crud?id=<%= dataTable[i].id %>"><button type="button" class="btn-edit">Edit</button></a>
          <a href="/delete-crud?id=<%= dataTable[i].id %>"><button type="button" class="btn-delete">Delete</button></a>
         </td>
       </tr>
    <% } %>
    </table>
</body>
</html>